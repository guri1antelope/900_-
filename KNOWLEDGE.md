# 面接アプリ 技術・運用知見（KNOWLEDGE）

## 1. 音声技術（Web Speech API）の挙動と注意点
- **ブラウザ対応**:
  - 音声認識（`webkitSpeechRecognition`）: Google Chrome, Microsoft Edge等 Chromium系ブラウザで安定動作。
  - 音声合成（`speechSynthesis`）: 主要モダンブラウザで広く利用可能。日本語音声（`ja-JP`）を選択して自然な発話を促す。
- **マイク権限とローカル実行（file:// プロトコル）の制約と対策**:
  - `file://` で直接開く場合、Chromeはセキュリティ仕様上オリジンが存在しないため「サイトへのアクセス時は許可」を選んでも永続記憶されず、マイクの再起動ごとに許可ダイアログが出る。
  - **対策1（コード側）**: `app.js` 内で設問ごとにマイクを停止・再開せず、セッション中は常時稼働させて先生の発話中のみ音声入力を無視するフラグ制御に変更（面接中の度重なるダイアログを防止）。
  - **対策2（起動環境）**: `面接アプリ起動.bat` を用意し、ローカルサーバー（`http://localhost:8000`）として開く。localhostであればChromeが「常に許可」を記憶するため、起動時も含めて一切ポップアップが出なくなる。
- **無音判定と誤認識対策**:
  - 小学生の話し始め・考え中の間（沈黙）で音声認識が勝手に終了してしまわないよう、`continuous = true` の設定や「回答完了」ボタンでの明示的な終了判定を併用する。
- **スピーカー音の文字起こし混入対策**:
  - 音声合成（先生の質問）がマイクに拾われて文字起こしに入り込まないよう、先生の発話終了時を認識開始インデックス（`recognitionStartIndex`）として設定し、それ以前の音声バッファを完全に破棄する制御を採用。
- **固有名詞の音声合成（読み仮名）対策**:
  - 「高志（こうし）」をTTSエンジンが「たかし」と誤読する現象を防ぐため、画面表示は漢字を維持しつつ、発話テキストのみ「こうし」にフリガナ置換して渡す前処理を実装。
- **自然な肉声読み上げ（Edge-TTS連携）**:
  - ブラウザ標準の機械的・平坦なロボット声を解消するため、無料AI音声エンジン（`edge-tts`）を用いたPythonバックエンド（`server.py`）を構築。
  - 声質: `ja-JP-KeitaNeural`（落ち着きと温かみのある大人の男性の先生声、速度-5%で小学生が聞き取りやすい設定）。
  - 音声キャッシュ機能により、同一設問の2回目以降の再生は遅延ゼロで再生可能。

## 2. 高志中等教育学校 入学者選抜の評価ポイント
- **「自立と互恵」の実践経験**:
  - 単なる優等生的な答えではなく、「自分で考えて行動したこと（自立）」「友達や周囲と助け合ったこと（互恵）」が具体的なエピソードとして入っているかが最重要。
- **灰野校長の重視項目**:
  - 「小学校6年間で何をやり遂げたか（継続・努力）」
  - 「客観的な自己評価（長所だけでなく短所をどう改善しているか）」
- **話し方の3原則**:
  - 結論ファースト（なぜなら〜）
  - 丸暗記ではなくキーワードで自分の言葉にする
  - 落ち着いた姿勢・ハキハキとした挨拶
